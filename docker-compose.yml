services:
  game:
    # Image built and pushed by GitHub Actions on every push to main.
    # Replace YOUR_GITHUB_USERNAME with your actual GitHub username.
    image: ghcr.io/juanedcabrera/jeca-game:latest
    restart: unless-stopped
